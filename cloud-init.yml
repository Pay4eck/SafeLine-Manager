#cloud-config
runcmd:
  - echo "SafeLine Manager 0.1 cloud-init is intentionally disabled; use docs/LINUX_SMOKE_TEST.md with an exact commit."
  - /usr/bin/false

# The inherited file cloned an unpinned Hiddify default branch. SafeLine does
# not provide a production cloud-init/update channel yet.
